# Cutover — implementation progress

Implementation goal started 7 September 2026. The implementation plan and all 54 acceptance scenarios remain the completion contract. Research is the baseline; runtime results below must come from executed checks.

| Phase | Status | Evidence / next work |
| --- | --- | --- |
| 0. Setup and compatibility | Initial compatibility verified | Git/public remote, checksums, backend, Kubernetes, identity, browser and telemetry smoke gates pass. Full clean/offline verification remains phase 8. |
| 1. Walking legacy system | Baseline verified | Stored routines, trigger-created tasks, polling scheduler, adapter journal and independent ledger pass component and real process checks. |
| 2. Reproducible packaging | Baseline verified | Clean Maven verification, reproducible generated SQL types, local images, JWT/mTLS workflow and process restart checks pass. |
| 3. Local platform | Initial milestone verified | Dedicated kind/Calico, frozen baseline transfer, 44 traffic checks, authenticated console, seven metric targets and retrievable traces. |
| 4. Reliability boundary | Foundation verified | Transactional messages, bounded admission/retries, uncertainty, cancellation and retained-history guards pass component/process checks. Physical-volume pressure and the full acceptance profiles remain phase-8 gates. |
| 5. Extraction and shadow | Foundation verified | 1,000 real shadow comparisons; 33 original tasks retained through the guarded boundary; assignment-created execution tasks now operate after a real owner switch. |
| 6. Cutover and rollback | Foundation verified | 105 full-suite checks; live drain/fencing, concurrency and uncertain-outcome reversal; all five actual process restarts; previous adapter image on V115. Failed measurements and repairs remain recorded. |
| 7. Second product | Foundation verified | 121 full-suite checks including 16 returns checks; five deployed workflow checks, two browser checks, ten metric targets and 83 network-policy checks. Separate receipt/counter/task ownership is verified. |
| 8. Full operations verification | In progress | Six-database capture, interrupted capture, fresh/stale restoration and preserved-copy restart verified. Actual bounded filesystem pressure and deployed observation expiry pass. Measured capacity, prepared offline operation and the final acceptance bundle remain. |
| 9. Portfolio finish | In progress | Real console workflows and public source exist. The architecture guide, current README and reviewer path are written; final screenshots, complete evidence mapping and the timed walkthrough remain. |

## Setup observations

- Existing Java 21, Node 24, Docker Linux engine, Git, GitHub CLI, and kubectl were found.
- kind and global Maven are absent. Install kind locally to the project tools cache; supply Maven Wrapper.
- The owner authorized pausing one unrelated local workload. Its 13 containers were stopped with volumes preserved; other workloads remain running. The Docker VM subsequently reported about 12.8 GiB available memory, including reclaimable cache. Host and VM free memory must be measured separately. The exact local workload permissions are retained in ignored operational notes.
- Git was initialized with baseline commit `90bcba5`. The public repository is [fullstack-nick/cutover](https://github.com/fullstack-nick/cutover); GitHub Actions is disabled. Only reviewed public source was pushed.
- Maven 3.9.16 and kind 0.33.0 were acquired with verified checksums. Maven Wrapper 3.3.4 pins the distribution SHA-256. Java dependency resolution and compilation succeeded.
- The runtime smoke tests pass PostgreSQL/Flyway/jOOQ generation and compilation, Boot HTTP JSON, JSON Schema formats, mandatory returns, and bounded quorum rejection. The initial capacity assumption was corrected after observed behavior; see ADR 0001.
- No full acceptance scenario is declared passed yet. Component evidence is not a substitute for the required final platform/failure/security/portfolio runs.

## Verification record

Record each milestone's command, result, tested commit, and evidence path here. Raw machine details and credentials belong under ignored `.local/` storage. A blocked or failed check remains visible until rerun successfully.

### Legacy component baseline — 7 September 2026

`./mvnw.cmd -B -ntp test` completed successfully at 19:19 Europe/Berlin in 1 minute 54 seconds: 3 compatibility checks, 5 simulator checks, 5 adapter checks, and 7 connected legacy workflow checks; no failures, errors, or skipped tests. Raw build output is retained locally. Each suite uses disposable PostgreSQL 18.6 databases; broker compatibility uses RabbitMQ 4.3.5.

The connected workflow tests use real owner databases, routines, constraints, task polling, command state machines, and physical ledgers. Their transport ports are deterministic in-process test adapters. Actual HTTP, JWT identity, mutual TLS, container restarts, and network-policy evidence are the next gates and are not implied by these results.

Corrections found by these checks included explicit timestamp casts in plain SQL, unambiguous bind-marker spacing around PostgreSQL operators, and using the injected clock for initial command deadlines. The tests retain the database assertions that exposed them.

### Reproducible process baseline — 7 September 2026

`./mvnw.cmd -B -ntp clean verify` passed all 20 tests with no failures, errors, or skips in 2 minutes 37 seconds at 20:02 Europe/Berlin. Every app generated its SQL types against disposable PostgreSQL using its actual Flyway migrations. Repeating generation and packaging produced identical SHA-256 hashes for all 98 generated Java files. Representative runtime queries now consume those generated types.

`./scripts/dev.ps1 -Action Start -SkipBuild` and `node tools/scenario-driver/baseline.mjs` passed on the rebuilt images. The six process checks establish real Keycloak authentication, HTTP intake/idempotency, service-to-service HTTP, simulator mutual TLS, site and signature rejection, authenticated runtime DDL denial (including Keycloak), one physical/inventory effect after a lost response, and blocked accepted work surviving restart of core, adapter and simulator. Evidence is recorded in ignored run `baseline-1788804441006`, with exact image IDs, jar hashes, and source revision `0ca4677` plus the then-uncommitted packaging changes. These results are development-profile milestone evidence, not final A01–A54 acceptance.

Startup tests found three configuration defects: an unquoted comma in a YAML tmpfs option, a Windows-reserved simulator port, and a missing `/identity` path in the advertised issuer. They also exposed Docker's internal-network-only port-publication behavior. All were corrected and the complete process suite rerun. See ADR 0002 and the development-baseline runbook.

### Local platform milestone — 7 September 2026

The dedicated kind 0.33.0 / Kubernetes 1.36.4 cluster uses Calico 3.32.2 with VXLAN, explicit policies and project-local image digests. Five frozen application databases were transferred from the development profile into empty cluster databases; the independent simulator and its physical history were preserved. Runtime role grants were reverified after restoration.

`platform-smoke.mjs` passed four checks in run `platform-1788806310326`: fresh simulator evidence through the in-cluster adapter, an authenticated ambient/chilled order with one physical and inventory effect per movement, site restriction, and runtime DDL denial. `network-policy-smoke.mjs` passed all 44 checks in run `policy-1788806688461`. `telemetry-smoke.mjs` passed three checks in run `telemetry-1788808276745`: seven expected scrape targets, an indexed/retrievable application trace, and Grafana database health. Evidence retains revision `579ff51` plus the uncommitted platform changes and exact image inventories.

Playwright CLI verified local PKCE login, real order/reservation detail, command-journal display, equipment navigation, Escape dismissal and a 390-pixel viewport without document overflow. The browser reported no console warnings/errors in the exercised flow; a storage-key check found no persistent token key. Screenshots are local development evidence, not final portfolio images or full A51 acceptance. The incomplete first-login human profile, narrow sidebar overflow, stale forward after rollout and loss of a requested view across identity redirection were found during this work and corrected in source.

The initial single-platform image import, Calico liveness and Tempo writable-path failures are recorded in ADR 0003 with the successful corrective configuration. The first telemetry assertion used the wrong Prometheus label; the corrected check follows the actual configured job/service labels. No full A01–A54 acceptance claim is made from these milestone checks. Extraction, migration, returns, recovery and the complete failure suite remain required.

### Durable messaging foundation — 7 September 2026

`./mvnw.cmd -B -ntp verify` passed 35 tests with no failures, errors or skips in 5 minutes 7 seconds at 21:47 Europe/Berlin. This includes 13 real PostgreSQL/RabbitMQ reliability checks and two additional owner-transaction checks. The latter force failure after inventory consumption or allocation registration and verify that those effects roll back with the inbox transaction before a successful retry.

The reliability checks cover commit/confirm/acknowledgement crash boundaries, single effects after duplicate transport delivery, mandatory returns, quorum rejection without dropping confirmed messages, stream gaps/conflicts, malformed/source/site quarantine, retry exhaustion, audited replay, capacity refusal and worker pause. Early runs caught plain-SQL bind parsing and JSONB casts; an idempotent response test exposed differing numeric node representations after storage. Those failures were corrected before the passing run.

The technical migration was moved to V101 so an existing V100 owner schema can upgrade in order. A focused clean run then passed 14 reliability checks, including a previous-schema upgrade that retains business rows. Clean packaging regenerated each application's SQL types and produced updated images. No original applied migration was edited.

The updated images deployed into the existing cluster without clearing application or physical data. `messaging-smoke.mjs` passed two process checks in run `messaging-1788811240256`: retained source streams drain into their receiving owners, and a new ambient/chilled order records applied intent/completion inboxes with exactly one physical and inventory effect per movement. This used source revision `354bb4e` plus the then-uncommitted messaging changes. Evidence now separately records actual running pod and simulator image identities, in addition to the build inventory.

Configuration-content hashes now trigger workload rollouts for mounted files and environment secrets. `messaging-smoke.mjs --broker-outage` then passed all three process checks in run `messaging-1788811360770`. RabbitMQ's pod was stopped with its volume preserved; an order was accepted with a nonempty durable outbox, and the source streams drained after the broker returned. Actual inventory/physical uniqueness assertions passed for the normal order in the same run. This small outage run does not replace the full capacity/exhaustion scenario. The remaining equipment/reconciliation/admission work is still in progress; phase 4 is not complete.

### Equipment evidence and process crash boundaries — 7 September 2026

`./mvnw.cmd -B -ntp verify` passed 45 checks in 3 minutes 34 seconds at 22:40 Europe/Berlin: 3 compatibility, 17 reliability, 7 simulator, 9 adapter and 9 connected core checks, with no failures, errors or skips. A preceding focused equipment run passed 16 checks. The first full run exposed an outdated assertion that assumed exactly one new migration after V100; the upgrade check now verifies the required V101/V102 versions and preserved business rows without forbidding later additive migrations.

The adapter now retains valid proof separately from stale/conflicting observations and records an acceptance tombstone. Tests prove executing work cannot regress on an old accepted observation, contradictory same-version contents are quarantined, known acceptance cannot be replaced by a later absence claim, and an audited investigation neither creates a new command ID nor reopens a terminal command. Simulator holds, delayed/repeated evidence and history/world response faults are durable, selectable, inspectable and clearable. See ADR 0005.

Rebuilt images and additive V102/V103/V104 migrations were applied to the existing environment. The independent simulator advanced from its earlier V100 schema with its world ID, journal generation and physical ledger preserved. `process-crash-smoke.mjs` passed three real pod-process checks in run `process-crash-1788814171314`: each deliberately fired the selected boundary, recorded exit code 73 and an increased restart count on the same pod, and finished accepted work with one physical/inventory effect. The confirm-before-mark and effect-before-ack cases observed repeated inbox delivery.

`equipment-recovery-smoke.mjs` passed six process checks in run `equipment-recovery-1788814389493`: lost response, failure before acceptance, simulator restart while executing, two incompatible-history investigations, and independent immutable-payload rejection by adapter and simulator. Real PKCE sessions checked operator denial, cross-site rejection, supervisor version/key semantics and one durable recovery audit. These runs used source revision `fd500dd` plus the recorded uncommitted changes and exact running image IDs. Their A-ID candidates are milestone evidence; the complete final acceptance bundle is still pending.

Continued operation exposed repeated Tempo OOM terminations under its initial 512 MiB limit. Business crash/recovery checks proceeded successfully during that monitoring failure. The local trace buffers and query concurrency were reduced, a 768 MiB Go soft limit was set inside a 1 GiB container limit, and only the owned failed Tempo pod was recreated with its PVC retained. The repaired process was observed at about 212 MiB working set and 169 MiB RSS, with no restart at that observation. `telemetry-smoke.mjs` passed all three checks in run `telemetry-1788814545498`. This short observation is not the required phase-8 capacity run or a guarantee about later memory peaks.

Remaining phase-4 work includes cancellation without unsafe reservation release, total storage/admission pressure, and complete quarantine/repair diagnostics. Extraction, shadow, migration, returns, stale restoration, prepared offline operation and all final A01–A54 evidence remain required. Compatibility of old writers with the new command-evidence columns must be included in the selected N−1 image gate; additive schema shape alone is insufficient.

### Cancellation fence and restartable reservation release — 7 September 2026

The focused owner verification passed 7 simulator, 10 adapter and 13 core checks at 23:21 Europe/Berlin. After adding recovery from a new browser session and the concurrent-supervisor case, `./mvnw.cmd -B -ntp -pl apps/legacy-core -am -Dtest=LegacyWorkflowTest -Dsurefire.failIfNoSpecifiedTests=false verify` passed all 15 core workflow checks at 23:32 with no failures, errors or skips. These checks use real owner PostgreSQL databases; controlled ports reproduce response loss and an expired query lease. The new tests establish all-or-nothing reservation release, late-intent fencing, no send from an expired worker after cancellation, retained recovery after a lost certificate response, retry-budget exhaustion and version-checked resumption by a different supervisor.

The public and internal contracts now describe cancellation and retry; the console generates its types from them and builds successfully. The deployed V105 adapter and V106 core migrations preserve existing data. `cancellation-smoke.mjs` passed all three process checks in run `cancellation-1788817068207`: the real PKCE supervisor UI cancels two unstarted lines with one release each, operator/cross-site requests are refused, a physically executing movement rejects cancellation and later completes once, and concurrent requests plus changed-payload key reuse cannot duplicate stock release. Evidence records source revision `5b0bbe9` plus the cancellation changes and the actual running image identities. The independent simulator retained its existing world and history.

See ADR 0006 and the order-cancellation runbook. The remaining phase-4 work is retained-storage pressure and complete quarantine/repair diagnostics. This milestone does not declare the complete A08 or A51 final acceptance bundle, or any later phase, finished.

### Retention, quarantine repair and checkpoint write guards — 7–8 September 2026

`./mvnw.cmd -B -ntp verify` passed 59 checks in 4 minutes 10 seconds at 23:52 Europe/Berlin on 7 September: 3 compatibility, 22 durable-delivery, 7 simulator, 10 adapter and 17 core workflow checks; no failures, errors or skips. The new cases exercise the independent retained-outbox cap with actual committed payloads, seven-day retention, permanent duplicate identities after payload compaction, original-byte transfer after a source-configuration correction, cross-site/malformed quarantine refusal, and worker freeze. An allocation response arriving after the core is frozen cannot update task state or start a command. Critical core storage prevents allocation/dispatch while preserving accepted reservations.

Additive V107 was deployed to core and adapter without resetting their databases or the independent simulator. The first real diagnostic run failed because the shared internal-path security filter still required a service role, despite the controller's intended human permissions. Specific platform diagnostics and message-recovery paths now admit their documented roles; other internal endpoints retain their service restriction. The corrected source was repackaged, rebuilt and deployed before rerunning the process checks.

`storage-repair-smoke.mjs` passed four checks in run `storage-repair-1788819273019`, recorded at 00:17 Europe/Berlin on 8 September. A real platform-administrator PKCE session reads owner storage diagnostics while operator/supervisor access is denied. The explicit critical-storage fault rejects new intake with `Retry-After`, stops dispatch and retains an accepted reservation; clearing it produces one inventory and physical effect. A missing stream predecessor exhausts six local attempts with one broker delivery, then the actual predecessor publication plus an audited supervisor replay applies the original event. Malformed original bytes remain in the site-less diagnostic pool, and a publish attempt under real core broker credentials cannot forge the adapter exchange. Inventory/physical counts remain unchanged after that rejection. The probe runs as a separately invoked bounded process with the selected owned pod's restricted broker identity, and only with demo/test controls enabled.

The passing process run records revision `e98fb28` plus the reviewed reliability changes and exact runtime images. The earlier failed run is retained as `storage-repair-1788818644268`. Logical counters and an explicit critical-state fault are not physical-volume exhaustion. Host/PVC free-space guarding, realistic capacity/exhaustion profiles, full checkpoint restoration, prepared offline execution and the complete final A01–A54 bundle remain required. No accepted business record or unresolved payload was removed to clear pressure.

### Execution owner scaffold — 8 September 2026

`node scripts/scaffold-service.mjs execution-service execution` generated the service from the committed technical template and recorded its template hashes. The template contains infrastructure and migration configuration without a fulfilment or returns model. The new service then added its own task table and adapter-assignment handler. It has no stock/reservation tables and no runtime dependency on the core's Java module.

`./mvnw.cmd -B -ntp -pl apps/execution-service -am test` passed seven checks at 00:17 Europe/Berlin: four real PostgreSQL assignment/ownership checks and three pure scheduling checks. Duplicate assignments have one task effect, other owners remain observations, changed assignments quarantine, and ordinary core publication cannot grant execution ownership. The proposal preserves immutable input and uses canonical unsigned UUID ordering, compatible lanes and explicit rejection reasons for recorded commands or stale evidence. A subsequent focused run passed all three proposal checks at 00:21 after adding the missing-epoch rejection.

This is the beginning of extraction. The execution service is not deployed or dispatching, the legacy trigger remains active, and the live route still belongs to the legacy coordinator. Registering the existing inventory, removing trigger-created tasks through an explicit migration, wiring the independent worker and shadow identity, and comparing at least 1,000 persisted identical SQL/Java inputs are the next required work. These seven checks do not establish A27, A28 or a completed extraction.

### Persisted decisions and running shadow — 8 September 2026

`./mvnw.cmd -B -ntp verify` passed 72 checks at 00:59 Europe/Berlin in 4 minutes 33 seconds: 3 compatibility, 22 delivery/reliability, 7 simulator, 10 adapter, 19 core, 7 execution and 4 shadow checks. The shadow component case persists and compares 1,000 seeded inputs through the actual PostgreSQL function and independent Java code, exporting every input/hash/proposal. New checks also exercise the dedicated authenticated observation exchange, duplicate delivery, separate capacity, excess timestamp precision, real priority dispatch and versioned recovery after six exhausted task transport calls. The console type generation, type check and production build passed.

Initial checks caught two defects before deployment. PostgreSQL rounded nanosecond input to microseconds, sometimes moving the stored observation slightly ahead of the decision clock; capture/storage now use a shared microsecond contract. A SQL bind marker adjacent to a comparison operator was misparsed by the SQL library; spacing the marker fixed the comparison-capacity update. The affected regression checks and full suite then passed. Packaging subsequently compiled the bounded in-pod identity probe and a null-input validation improvement.

The updated core, adapter, passive execution application and isolated shadow deployment rolled out successfully. `node tools/scenario-driver/shadow-smoke.mjs` passed three running-process checks at 01:10 Europe/Berlin, run `shadow-1788822421687`. All 1,000 seeded inputs were admitted by the actual core endpoint, persisted with its SQL proposal, published through RabbitMQ, compared by the shadow Java process and read back through the authenticated site API. Every input/hash and both outputs matched; zero differences occurred. The full export's SHA-256 is `9123b89ee81f38956a6d8fcf0414f98d467637fee070922062845ead19e0f78e`. A real ambient/chilled order then produced four additional matching live comparisons, two inventory effects and two physical executions. The passive execution consumer applied 100 adapter observations without creating an owned task.

The actual shadow pod launched a bounded HTTP probe with `CUTOVER_SHADOW_MODE=false`, retaining its separate credential. Adapter command and allocation requests both reached HTTP and returned 403 (`DISPATCH_IDENTITY` and `INTENT_SOURCE`). No command or physical execution was added. The process evidence records source revision `301bded` plus these reviewed working changes and the exact runtime image identities. See [the selected shadow evidence](../evidence/shadow-2026-09-08.md).

Before this rollout, a read-only inventory found 31 legacy tasks and 31 matching adapter allocations, with no missing movement. Three cancelled legacy rows have no local allocation link yet, although their adapter cancellation tombstones exist. The original task IDs and inventory were retained privately for the upcoming paused registration boundary. The trigger remains active, both outbound routes remain legacy-owned, and the execution application still has no active dispatch worker. Controlled task extraction, durable zone migration/reversal, compatible rollback, returns, shadow replay/retention controls, physical storage pressure and all final acceptance/portfolio gates remain required.

### Guarded registration and independent worker — 8 September 2026

The initial boundary now records bounded adapter receipts and a complete core checkpoint while intake and dispatch are paused. Core V112 refuses to change the original trigger without matching, settled task evidence at the observed control version. Its intent-only trigger preserves the existing task rows, reservation routine and characterized priority function. Subsequent adapter assignments create the relevant owner's task. The execution worker writes only its own task/decision tables, observes recorded commands before considering dispatch, and exposes bounded transport retry/recovery. See ADR 0009 and the initial-task-boundary runbook.

Focused checks passed 29 core tests at 01:47 and 13 execution tests at 01:52 Europe/Berlin. `./mvnw.cmd -B -ntp verify` then passed all 88 checks in 5 minutes 40 seconds at 01:59, with no failures, errors or skips: 3 compatibility, 22 delivery, 7 simulator, 10 adapter, 29 core, 13 execution and 4 shadow checks. The shadow fixture again compared 1,000 persisted identical inputs. The console contract generation, type check and production build passed.

The connected tests use real independent owner databases and deterministic transport ports. They establish missing/stale registration refusal, original ID preservation, assignment-created tasks, mixed task owners, lost adapter responses, retry-budget recovery, checkpoint freeze, unknown physical outcomes, and observation-quota isolation.

The verified images deployed first with core migrations capped at V109. The paused registration verified all 33 original tasks at 02:07 Europe/Berlin, preserving their IDs and linking the three cancelled tasks to their retained allocation tombstones. Inventory hash: `9ddc4c8af12ed6a3fd3828d8db4a697f19889ed66486471d0d82ebc24b4c7844`. The normal latest-schema deployment then applied guarded V112 successfully. The saved release operation verified the new trigger and reopened the original gates. Simulator history and both legacy-owned route epochs were preserved.

`assignment-boundary-smoke.mjs` passed three real process checks in run `assignment-boundary-1788826201899` at 02:10. With adapter processing paused, a new order retained two intents and had zero tasks/allocations. After resumption, RabbitMQ assignments created two legacy tasks with the characterized priority; both movements recorded one physical and inventory effect. Execution applied the same assignment observations without creating other-owner tasks. Its public read route enforced site scope and its runtime role could not perform DDL. The first run completed its order but failed a read-only evidence query that attempted to execute a restricted routine; the assertion now inspects the stored priority, without broadening observer grants.

This used source revision `d77237b` plus the reviewed changes and exact running image identities. See [selected boundary evidence](../evidence/assignment-boundary-2026-09-08.md). No live owner switch is claimed yet. Durable zone migration, returns, recovery/offline/capacity and the full final acceptance bundle remain required.

### Durable zone sessions and retained reversal evidence — 8 September 2026

Adapter V114 introduces supervised, versioned migration sessions with a strict allocated-work barrier, bounded immutable inventory, owner proof pages, a physical-world checkpoint and an atomic route epoch switch. New work waits unassigned during the drain. The first ten new-epoch movements must have single physical/inventory effects and meet the two-second dispatch target. A failed sample remains visible and requires an explicit reversal. The console exposes the current routes, blockers, proof hashes, actor/reason, phase and bounded recovery controls. Core and execution supply only their own owner evidence through adapter-restricted APIs. See ADR 0010 and the zone-migration runbook.

The full backend verification passed 102 checks at 03:08. A later `./mvnw.cmd -B -ntp verify` passed **105 checks at 04:24 Europe/Berlin in 7 minutes 57 seconds**, with no failures, errors or skips. The latter adds nested-reversal settlement and empty-poll durability checks to the existing 1,000-comparison, delivery, cancellation, ownership and equipment cases. The console contract generation, type check and build passed.

Real process evidence includes an ambient switch to execution with a 592.983 ms sample p99, delayed old-owner rejection, two actual supervisor identities contending for one route version, an unknown command blocking reversal beyond its transport timeout, and a completed chilled reversal with a 297.346 ms sample p99. These selected checks and their earlier harness failures are recorded in the zone-migration evidence document. They are implementation milestones, not a complete final A01–A54 bundle.

Three retained latency misses—2,981.393, 2,784.272 and 2,420.441 ms—prompted measured corrections. RabbitMQ's repeated CLI readiness probe was replaced with TCP readiness after observed CPU throttling. Empty worker polls then proved to cause substantial WAL flushing. Read-only fast paths keep the existing freeze/lease/admission checks inside real mutation transactions. After rollout, the idle database diagnostic dropped from about 125 to 1.009 client-backend WAL fsyncs per second, with synchronous commit and fsync enabled. See ADR 0011.

Additive V115 preserves assignment timestamps for the tested predecessor writer and closes nested reversal lineages with distinct `REVERSED` and `SUPERSEDED` outcomes, without replacing failed samples. The first simulator traffic check exposed an omitted V107 shared migration in its independent database; command admission failed before physical acceptance. The explicit simulator updater applied V104→V107 with its data volume retained. Supervised reconciliation then completed the same command once in both physical and inventory ledgers. This failure and correction remain recorded.

The live five-phase restart check then passed in `migration-crash-1788834766024-resume-1788835287007`. The same session retained 87 inventory proofs, five actual exit-73 records and one epoch increment; its ten-movement dispatch p99 was **552.919 ms**. The missing simulator migration was repaired and the original command reconciled before resuming the saved run. Both earlier failed reversal observations remain unchanged; the immediate parent is `REVERSED` and its ancestor `SUPERSEDED`.

`adapter-image-rollback-1788835517276` passed the selected N−1 gate: the cached phase-5 adapter operated against V115 and completed six new movements once each, with every route owner, epoch, version and schema version unchanged. The current image and dispatch gate were restored. Real supervisor/operator browser checks then verified the completed session, retained failed history, role controls and narrow layout. See the selected zone-migration evidence and application-image rollback runbook. These are phase-6 milestones; returns, full restore/offline/capacity work and the complete final acceptance bundle remain required.

### Independent crate returns — 8 September 2026

The returns application was generated from the technical scaffold and given an owner-local V116 schema, transactional receipt registration, single-effect sorting ledger, and bounded coordinator. No fulfilment persistence model or scheduler is a runtime or connected-test dependency. All four recorded template hashes were verified against their sources.

`./mvnw.cmd -B -ntp verify` passed 121 checks in 7 minutes 53 seconds at 05:24:51 Europe/Berlin, including 16 returns checks. `returns-1788838277326` passed five deployed checks: duplicate HTTP references and distinct broker event IDs, mixed destinations and counters, site/role/input boundaries, lane independence, a returns process outage while outbound continued, and physical lost-response recovery. The actual V116 runtime and original receipt/task/movement identities were verified.

`returns-ui-1788838637610` passed two browser walkthroughs after wording polish: PKCE, current totals and receipt evidence, keyboard dismissal, a 390-pixel viewport, operator restrictions and supervisor reconciliation through the form. `telemetry-1788838441512` verified ten scrape targets plus retrievable traces and Grafana health. `policy-1788838595392` passed 83 actual DNS/TCP checks, including the new product and the explicit migration proof paths.

Postflight found all six test receipts complete, ten single sorting effects, all crate counters reconciled and the same simulator world/generation at sequence 179. Site-a outbound owners/epochs remained unchanged. The public [returns evidence](../evidence/returns-2026-09-08.md), [onboarding guide](../onboarding/returns-service.md), [ADR 0012](../adr/0012-independent-crate-returns.md) and [runbook](../runbooks/returns.md) explain this milestone and its limits. Full mixed-product capacity, physical storage pressure, backup/restore, prepared offline operation and the final acceptance bundle remain phase-8 work.

### Consistent application capture and interrupted maintenance — 8 September 2026

The write-barrier audit found missing freeze checks in new idempotency records for an existing order reference, manual command investigation and fault controls. Those paths now reject writes while frozen, retain previously committed responses for reads, and allow an explicit versioned resume. All 125 backend checks passed at 06:09:41 Europe/Berlin in 9 minutes 5 seconds. The applications were rebuilt and deployed without resetting data.

The checkpoint procedure freezes/stops five application writers plus Keycloak and captures six databases with 207 per-table fingerprints, source replay identities, image/schema/route metadata and current simulator journal observations. Atomic local recovery journals retain each intended control request and writer stop. Private artifact permissions, exact-resource checks, checksums and versioned cleanup are enforced.

`checkpoint-1788841302213` passed three deployed checks at 06:24:09: complete capture/resume, corruption/path rejection, and real checkpoint-process termination at the stopped-writer boundary followed by journal-based recovery. Capture plus resume took 77.243 seconds; the stopped-writer dump/check interval took 16.400 seconds. All selected business rows and physical world/generation/sequence 179 remained unchanged; one existing adapter quarantine was preserved.

An earlier export included controller-generated EndpointSlices. The corrected exporter excludes them and the verifier rejects the original artifact as unqualified for restoration. The full selected process run was repeated. See the [checkpoint evidence](../evidence/checkpoint-2026-09-08.md), [runbook](../runbooks/checkpoint.md) and [ADR 0013](../adr/0013-stopped-writer-application-checkpoints.md). This milestone is not full A46/A47 restoration or A52 stop/reset acceptance; those and the remaining phase-8/phase-9 work are still required.

### Application restore implementation — 8 September 2026

The restoration implementation adds an independent adapter gate, paged physical-history and accepted-command inventory checks, retained same-world absence proofs, original-event replay and a separately owned restoration cluster lifecycle. Missing business intent keeps dispatch held. A known completion arriving after verification expires the proof so the same session can extend it. Restore never resets the simulator or invents missing application work. See [ADR 0014](../adr/0014-physical-history-aware-application-restoration.md) and the [restore runbook](../runbooks/restore.md).

All 141 backend checks passed at 07:35:18 Europe/Berlin in 8 minutes 7 seconds. A final adapter history-rewind guard and regression case then passed the 27 selected command-journal/restoration checks at 07:39:36 in 1 minute 29 seconds. These use disposable owner databases and the real simulator engine; delivery checks also use a real quorum broker. Initial checks caught a test JSON numeric representation mismatch and an in-flight adapter state-name mismatch, both corrected before deployment. The restore cache preflight verified the retained image archives; occupied Docker ranges required selecting a separately checked private subnet.

Deployment and fresh/stale checkpoint process acceptance are the next checks. No full A46/A47 result or restore timing is claimed by these component tests. The remaining phase-8 and phase-9 gates, including physical storage pressure, prepared offline operation, mixed-product capacity and the full A01–A54 evidence bundle, are still required.

### Deployed restoration and physical storage guard — 8 September 2026

`restore-1788849841860` passed four process checks: all six fresh imports and 211 table fingerprints, 1,207 original events replayed to a new broker, 183 matched physical and 183 matched command records, guarded release in 648.572 seconds, cold stop/start of the preserved restoration copy, real restored identity/product reads, and safe return to the original demo. `stale-restore-1788848295108` passed A47: one known outstanding return completed once; three later physical movements with absent checkpoint intent were quarantined; release was refused; the measured RPO interval was 516.516 seconds. Simulator world/generation remained unchanged. See the [selected restoration evidence](../evidence/restore-2026-09-08.md) for exact scope and retained failures.

The first restore's return-to-demo step opened a forward before fresh containers were ready; process-start-time readiness corrected it. A subsequent image import encountered CRI's delayed observation of a newly tagged digest; bounded digest readback corrected that failure. Both failed attempts remain recorded. The full cold lifecycle rerun passed after these corrections. This does not establish the separate destructive reset or final reviewer recording gates.

All **146 backend checks** passed at **09:01:47 Europe/Berlin**, in **11 minutes 1 second**, with no failures, errors or skips. They include real 384 MiB filesystem pressure, stale/malformed/missing probe behavior, restricted server-file/function permissions and missing schema capability checks. A preceding 27-check delivery/storage run passed at 08:47:12. Its first physical-pressure test passed, while the first probe-stop harness relied on an absent `pkill`; the corrected harness signals its own recorded probe PID.

`volume-pressure-1788851160214` passed the isolated A26 HTTP/process experiment. A real core JVM used its restricted database role and a disposable 384 MiB PostgreSQL filesystem. At 50,864,128 available bytes, below its 80,530,636-byte reserve, intake returned `503 STORAGE_OBSERVATION` with `Retry-After: 2`; the running scheduler recorded that barrier without an equipment-boundary request. The accepted order and reservation remained readable across a cold JVM restart. Removing only the fixture's filler restored admission; retrying the original refused request key accepted the second order. Exactly two orders/two reservations and zero inventory effects remained. This profile deliberately connects no live simulator; its equipment boundary is a counted refusal endpoint. Earlier attempts selected the thin JAR and disabled the messaging configuration containing diagnostics; both harness errors are retained separately.

The filesystem probe, fixed-path administrator-installed function, runtime grants, readiness/startup capabilities and deployment templates are implemented. New images and deployed probe verification are the next step. No existing applied business migration was changed. Full capacity/offline/security/UI/reviewer acceptance remains required.

The updated simulator and all five application services deployed successfully. `volume-observation-1788851871227` passed both platform checks at 09:18:19: real administrator/operator permissions across five storage endpoints, five single physical effects across outbound and returns, and expiry of the actual paused sidecar report after 15 seconds. Both products refused new intake while accepted reads and counts remained intact; resuming the same sidecar restored fresh evidence. World/generation and physical high water 193 remained unchanged during the outage. The first live outage attempt used a signal that did not suspend namespace PID 1; its failure is retained. The corrected test pauses/resumes the exact recorded containerd task.

The updated platform passed all 83 policy checks in `policy-1788852008554` and all three telemetry checks in `telemetry-1788852062838`, including ten actual scrape targets. Telemetry verification now establishes its own named loopback forwards; the initial attempt without the post-restore forwards remains recorded. Forward startup exposed a 60-second inherited-handle delay. The new detached helper passed actual stop/start, occupied-port refusal, substituted-process refusal and healthy-PID reuse checks; fresh Grafana startup took 979 ms in that check. These are lifecycle checks, not load/SLO measurements.

### Causal observation and retained-order recovery — 8 September 2026

All **150 backend checks** passed at **09:57:35 Europe/Berlin**, in **9 minutes 46 seconds**, with no failures, errors or skips. The four new checks cover W3C continuation, event causation/correlation through committed delivery and later worker lookup, context isolation and sanitized failure observations. A preceding focused run passed 29 checks at 09:46:27. See [ADR 0016](../adr/0016-causal-observations-through-durable-work.md).

The new images deployed with schemas and the simulator world retained. The deployment's final console-forward step encountered an already-exited process; the stop operation now rechecks process ownership and treats `ESRCH` as an idempotent exit. The original failure remains recorded. Forward recreation, identity fixture verification and all remaining application rollouts then passed.

The pre-load audit found one `RESERVED` order from the earlier failed migration run, whose original intent remained in the adapter's quarantine. `retained-intent-1788855010368` verified its unchanged source bytes, healthy current route, no allocation and same-world retained physical absence. Real supervisor login and audited replay then applied the original event and completed the original order with one physical/inventory effect. The original failed migration result remains unchanged.

`causal-trace-1788855048465` passed both deployed checks. A mixed-zone order and mixed-classification receipt produced five single physical/business effects. Tempo retrieved their original HTTP trace IDs with 633 and 840 spans, including event publication/application, task coordination, command recording/investigation and inventory/sorting completion. Exact observed service/image identities and private exports are retained. The separate sampler check `load-1788854250332` passed with no workload offered; an earlier sampler harness inherited an incompatible Node stdin option and is retained as a failed harness attempt. Sustained-load, offline and final portfolio acceptance remain separate gates.

### Sustained-load denominator audit — 8 September 2026

`load-1788855149799` accepted all 1,980 requests and completed all 3,300 movements with one physical/business effect each. Its original harness reported 99.733% within two seconds and a 482 ms p99 from the later task-created stage. Inspection found that original movement eligibility to adapter acceptance was only **67.3% within two seconds, p99 9,982.546 ms**. The original result remains unchanged, with a separate checksum-linked qualification note. It is insufficient for the final A50 qualification.

The load harness now uses original immutable movement eligibility for its primary gate, includes assignment/publication delay, retains the later task-stage metric separately and verifies every effect quantity and stock decrement. Bounded diagnostics explicitly make no A50 claim. The pre-fix diagnostic `load-diagnostic-1788856854826` completed 350 single effects but measured only **48.667% within two seconds, p99 10,304.704 ms**, with **185.055 client-backend WAL fsyncs/second** over the offering interval. Its first harness attempt rejected the new diagnostic run-ID format before offering work; that failure remains recorded.

Unarmed process-fault hooks acquired worker-control row locks on every normal delivery boundary. A read-only check now avoids those transactions while preserving the exact locked event match, freeze check, durable one-shot consumption and audit for armed faults. All **151 backend checks** passed at **10:52:13 Europe/Berlin**, in **9 minutes 5 seconds**, with no failures, errors or skips. The new PostgreSQL read-only regression verifies no assigned write transaction ID and notices later arming. Image rollout and the after-fix diagnostic are next; no improved runtime result is claimed yet.

### Sustained-load follow-up and control-barrier verification — 8 September 2026

The `3f49f9f` images deployed with existing data retained. The short after-fix diagnostic `load-diagnostic-1788858013964` measured 100% within two seconds and a 1,601.067 ms p99. The following complete run, `load-1788858153493`, **failed A50**: 83.333% within two seconds and a 9,312.635 ms p99 from original movement eligibility. All 1,980 requests were accepted and all 3,300 movements produced one matching physical/business effect; measured stock decrements matched. The later task-created stage reached 99.7% within two seconds and a 781 ms p99, which does not replace the original-eligibility gate. All raw attempts remain retained.

Read-only observations found WAL flush waits during aligned assignment delays. Increasing only the application PostgreSQL allocation from one CPU/512 MiB to two CPUs/1 GiB, with 256 MiB shared buffers, retained its volume, schema and simulator identity. Before/after 120-second diagnostics (`load-diagnostic-1788859514241` and `load-diagnostic-1788859974447`) measured 85.5%/9,802.285 ms and 86.333%/8,785.939 ms respectively. Sampled CPU throttling fell from 29.406 seconds to 0.233 seconds, but the dispatch target still failed. Durability remained `fsync=on` and `synchronous_commit=on`.

A transaction-scoped advisory control barrier is being verified to avoid write-generating row locks during ordinary permission checks. It must preserve the freeze boundary, reject stale transaction snapshots and use one lock order across runtime and restoration paths. The first concurrency test correctly reached the queued freeze but checked a nonexistent response field; its failed run is retained. The corrected backend suite is running. The console's new order register passed contract generation, TypeScript checking and production build; deployed browser acceptance remains pending.

All **155 backend checks** passed at **12:10:06 Europe/Berlin**, in **11 minutes 19 seconds**, with zero failures, errors or skips. The final barrier covers inbox admission, simulator work and baseline registration as well as ordinary workers and control writers. Restoration scripts take the same exclusive advisory key before row updates. The intermediate order-paging test incorrectly reused references containing spaces as request keys; valid distinct fixture keys corrected that test error. Both earlier failed logs remain retained. See [ADR 0017](../adr/0017-read-only-control-barrier.md).

The machine doctor passed with all required tools found. Its capacity check now includes the measured working set of the four existing, ownership-verified Cutover containers. The immutable source prefix checksum still matches and no `.local/` or workflow files are tracked. Broker-capacity, order-register and prepared-offline drivers are implemented and syntax-checked, but their new deployed acceptance runs have not yet executed. Image rollout, retained-data checkpoint verification and the next dispatch diagnostic follow this milestone.

### Relay throughput and the retained stock budget — 8 September 2026

Revision `63bdde7` built and deployed successfully with no schema reset. The simulator retained world `3b9c9c76-96ea-43e3-b009-af0c97939b35`, generation `d9f73d93-eff7-4cc7-a0db-238a6ff24571` and sequence 8,799 during replacement. Stock preflight found 1,352 chilled and 1,381 ambient units: enough for one full run, so the additional short diagnostic was omitted instead of replenishing stock or changing the workload.

`load-1788862813903` **failed A50** on the committed, clean `63bdde7` runtime. All 1,980 requests were accepted; all 3,300 warm-up/measured movements completed with one matching physical/business effect and exact stock decrements. Original-eligibility latency was **49% within two seconds, p99 29,519.439 ms**. The later task stage was 98.433%/2,719 ms. The measurement included all 3,000 eligible movements with zero exclusions; 2,944 completed within the offering window. Client-backend WAL fsyncs averaged 132.892/second, with 485.177 seconds of cumulative fsync wait over 660.595 seconds. This is not a successful capacity result.

One extra read-only snapshot during a backlog spike retained the oldest pending events in core, adapter and returns. Most had not been attempted and none of the selected rows recorded a broker error. The new relay implementation therefore batches already available stream-head claims and their database settlement while retaining individual positive publisher confirmations. It does not wait to fill a batch, allow stream overtaking, acknowledge an uncertain publish or weaken durability. [ADR 0018](../adr/0018-bounded-outbox-database-batches.md) explains the transaction boundary.

All **34 focused durable-delivery checks** passed at **12:42:32 Europe/Berlin**, in **1 minute 28 seconds**, against real PostgreSQL and a quorum broker. Five new checks cover stream order/atomic claims, partial failure and untouched unused leases, crash-after-confirm replay, freeze after publication and an expiring transport burst. The last full backend suite remains the 155-check run; no 160-check full-suite result is claimed. This batching change is not deployed or load-qualified yet. A fresh explicit seed reset, including preservation/ownership checks, is required before another complete load run because the retained dataset now has only 32 chilled and 61 ambient units.

### Preserved lifecycle and deliberate seed reset — 8 September 2026

The `6347cf5` relay images deployed successfully with existing owner schemas and simulator world retained. The normal demo lifecycle, fresh bootstrap and explicit reset scripts are implemented. Reset requires a verified current quiescent checkpoint and both destruction/new-world flags. It archives the isolated physical database before removing the exact recorded containers and their exclusively used volumes; unrelated workloads and private evidence are retained. The cached predecessor rollback driver now verifies single-replica `Recreate` replacement before changing images, which prevents overlap between the earlier and current control-lock protocols.

The first preserved lifecycle run, `demo-lifecycle-1788865617728`, restarted its services but failed a harness comparison because Docker returned unchanged volume mounts in a different order. Canonical ordering corrected that comparison. The next attempt found a real connection defect: Docker reassigned the simulator from `192.168.112.3` to `.2`, while the selectorless Kubernetes endpoint retained `.3`. A newly accepted return remained safely blocked with `EQUIPMENT_STALE`, with no command or physical effect fabricated. Start now refreshes the endpoint and the two narrowly scoped network-policy addresses, then waits for a fresh adapter observation of the same world. The retained receipt subsequently completed and active receipt count returned to zero. Both failures remain recorded. The complete corrected lifecycle rerun, destructive reset/fresh bootstrap, renewed load qualification and the remaining acceptance gates are still pending.

`demo-lifecycle-1788866307975` passed the complete preserved lifecycle check at 13:21:34 Europe/Berlin on committed lifecycle revision `6dbf524` and the `6347cf5` application images. Both Stop and Start were repeated. The four container IDs and sorted volume identities, all checked business counts, world/generation/high water 12,102 and the original receipt identity remained unchanged; a real operator PKCE login and reads succeeded after restart. This establishes the normal-preservation portion of A52, not its destructive reset gate.

`order-register-1788866584899` passed the deployed order-register walkthrough: literal case-insensitive reference search and site-wide shortage filtering, partial/total shortage states, one physical/inventory effect, command proof, operator control restrictions, Escape/focus restoration, retained rows under failed observations, empty results, and a 390-pixel viewport without horizontal page overflow. The preceding attempt correctly closed the dialog but used a role locator that excludes hidden elements while expecting its count to remain one; the corrected hidden-state assertion passed. Screenshots and the failed attempt are retained privately. The remaining task/recovery/migration/audit walkthrough is still a separate A51 gate.

The first reset/bootstrap attempt, `reset-bootstrap-1788866692673`, failed before any reset. Its checkpoint captured all six databases but the final physical-history read lost its TLS socket after the synchronous dump interval. Cleanup restored every prior writer/control state with no errors at 13:27:33. The read-only maintenance client now opens a fresh authenticated TLS connection for each observation and handles response-stream errors, avoiding reuse of an idle connection after long synchronous work. The failed checkpoint remains immutable and unqualified; a new complete capture/reset/bootstrap attempt is required.

`reset-bootstrap-1788866919366` passed at **13:41:28 Europe/Berlin**. The corrected checkpoint verified all six database dumps and resumed their prior controls. Explicit reset archived the old simulator journal, removed only its recorded Cutover containers/volumes and preserved every unrelated container ID and running state. Repeated reset was idempotent. The exact `bootstrap.ps1 -SkipBuild` command completed in **496.786 seconds** using the prepared `6347cf5` images. The fresh world is `086194fe-9210-4ca1-b468-896d4a8e91b4`, generation `6c482662-f64c-4157-9b24-6ba45604c7b7`; initial sequence zero and 200 stock rows/19,604 units/zero reservations matched the seed. A real operator login and both products then produced five single physical effects. Reusing the old reset against the new containers was refused. This completes the demonstrated normal-stop/explicit-reset scope of A52 and the prepared-image fresh-bootstrap portion of A54; final source quickstart and reviewer recording remain separate.

### Task, recovery and audit console — 8 September 2026

All **163 backend checks** passed at **14:00:40 Europe/Berlin**, in **12 minutes 23 seconds**, with zero failures, errors or skips. The new owner-scoped audit projection checks tied-time paging, foreign-site cursors, read-only access and omission of internal diagnostic detail. The adapter timeline reports retained event transitions and explicitly identifies incomplete history while keeping lifetime completion proof intact. Task projections prioritize active work and provide site-scoped individual lookups. No business schema or scheduling/dispatch mutation was added by these read projections.

The console now implements task queues/details, command timelines, reviewed supervisor investigation with reason/version and stable retry identity, four owner audit histories, and stored shadow input/output comparison. TypeScript and the production build passed; the complete deployed browser walkthrough remains pending. The bounded broker probe can publish up to 10,001 copies of a selected retained event using its actual publisher identity and at most 64 outstanding confirms; this is verification tooling, not a change to the runtime publisher protocol. Queue-capacity, prepared-offline and renewed full-load acceptance still require deployed results.

The clean `26ac729` images built and deployed with the physical world/generation retained. `work-console-1788869826870` passed at **14:17:53 Europe/Berlin**: real operator/supervisor PKCE sessions, site isolation, an actual competing supervisor version conflict, reviewed recovery of the same quarantined command, one physical/inventory effect, complete retained timeline, four owner audit readers, stored SQL/Java comparison and 390-pixel navigation. No browser errors were recorded. The working tree also contained the independently developing shadow-recovery change; the image manifest was clean `26ac729`, and that new recovery API was not part of this deployed test. Visual inspection found concatenated event labels; spacing between camel-case words is corrected for the next console build.

Queue-overflow preparation identified a missing operational action for exhausted shadow observations. A separate supervisor-only recovery API and additive core V113 recovery revision are implemented, preserving original event bytes and capacity while requiring reason, version, idempotency, no active lease and a paused/unpublished record. Its initial focused test did not compile because the test used the wrong exception constructor; that failed log is retained and the corrected focused run is in progress. The real queue-overflow driver is implemented and syntax-checked; no A16 pass is claimed yet.

The corrected shadow-recovery run passed all **five focused checks** at **14:19:17 Europe/Berlin** in 1 minute 39 seconds, including original-byte recovery, idempotency/version conflict, one comparison, unchanged business quota, checkpoint freeze and active-lease refusal. The full backend suite is running. Inspection also identified that its new internal route must sit under the existing messaging authorization matcher to admit human supervisors without widening general internal access; that HTTP boundary remains unqualified until the deployed queue test.

`shadow-1788869933650` passed all three deployed checks on the fresh world: 1,000 exact SQL/Java input/output comparisons with zero mismatches, live ambient/chilled work, and rejection of a shadow identity attempting to allocate or command even with its mode flag changed. `process-crash-1788870086499` then passed A11, A12 and A13 against the batched `26ac729` runtime. The actual core process halted with exit 73 at each selected boundary and restarted in the same pod; duplicate publication/acknowledgement delivery counts increased where required while physical and inventory effects stayed single.

`broker-capacity-1788870391246` passed **A15** at **14:30:26 Europe/Berlin**. With the actual Cutover broker stopped, 800 receipts were accepted and 1,600 original outbox events retained. The next two requests received `503 ADMISSION_CAPACITY` with `Retry-After`, before the hard quota. Restart reused the broker storage and created a new pod; one exhausted original delivery required an audited supervisor replay. All 800 accepted receipts completed, then the same two previously refused keys/bodies were admitted. The 802 movements produced exactly 802 physical and sorting effects with matching counters, zero active receipts/unpublished events and unchanged world/generation. Physical high water advanced from 11 to 813. The backend component suite was running concurrently; this is an outage/capacity result, not a healthy-load latency measurement.

`mandatory-return-1788870649419` passed **A14** at 14:31:13. The test removed one verified, journaled exchange/queue binding, retained two original business events after mandatory returns, and observed a positive-confirm returned publication using the actual restricted publisher. Restoring the same binding delivered the original IDs/bytes and produced one physical/sorting effect. No queue, message, broker pod or queue argument was replaced. The [RabbitMQ binding API](https://www.rabbitmq.com/docs/http-api-reference) supplies the exact binding properties key used for reversible removal and readback.

The renewed ambient migration `zone-migration-1788870698705` passed its operator, drain, independent chilled-work and former-owner fencing checks at 14:32:46, with ten observed new-owner movements and **857.687 ms** dispatch p99. `migration-crash-1788870805198` then passed all five actual adapter process exits and durable phase resumptions at 14:39:56; its ten-movement observation measured **1,990.183 ms** p99. Both outbound zones now belong to execution at epoch 1, with settled sessions. These samples are not the separate 10-minute A50 qualification.

All **165 backend checks** passed at **14:36:59 Europe/Berlin** in **16 minutes 11 seconds**, with zero failures, errors or skips. This includes the two new shadow-recovery checks. The subsequent route-only correction places its API under the established internal messaging authorization boundary; deployed human access still requires verification.

The new JWT tests exposed a real required-claim gap: Spring Security 7.1.1's default converter supplies `iat` from `exp` when the signed token omits it, so the later issued-at presence check was insufficient. The decoder now retains that absence through claim conversion. The [pinned upstream implementation](https://raw.githubusercontent.com/spring-projects/spring-security/7.1.1/oauth2/oauth2-jose/src/main/java/org/springframework/security/oauth2/jwt/MappedJwtClaimSetConverter.java) and local bytecode confirmed the behavior. The initial failed test is retained. All **seven service-starter checks**, including four new production-decoder tests, passed at **14:40:26** in 23.397 seconds: trusted-signature issuer/audience/time bounds, unknown keys, invalid/unsigned signatures, tampered site claims and unavailable JWKS. No live signing key or system clock was changed. These checks postdate the 165-check full run; no full 169-check result is claimed yet. The new deployed authentication/TLS/source driver is syntax-checked but not run.

### Full load, authentication and source review — 8 September 2026

`load-1788871306678` **failed A50** on the `26ac729` runtime: **78.067% within two seconds, p99 18,117.047 ms** from original movement eligibility, with all 3,000 measured movements included and zero exclusions. All 1,980 warm-up/measured requests were accepted and all 3,300 movements had exactly one matching physical/business effect. The later task-created stage reached 99.267% and 1,421 ms p99; it does not replace the original-eligibility result. Traces and retained outboxes locate substantial delay in publication and consumer queues. The original failed evidence is preserved.

`authentication-1788872591183` passed the live site/JWT, publisher-source and mutual-TLS checks. It also waited beyond the original access token's actual expiration, observed rejection across all six owner APIs, and verified a refreshed PKCE token succeeded. A genuinely signed local ID token with a non-API audience was independently verified and rejected. The world, journal generation and physical high water stayed unchanged. The full offline Maven run completed at **15:09:20 Europe/Berlin**, with **169 checks and zero failures, errors or skips**, in 13 minutes 15 seconds.

The static source review completed at **15:38:46** against `9ee083b`, covering all **430 tracked files** and reporting three low-severity, authenticated-local findings. Their corrections are described in [ADR 0020](../adr/0020-intake-and-recovery-capabilities.md). The original sealed report remains private and immutable. Actual Windows permission readback then showed inherited Users/Authenticated Users grants on local secrets; the ignored local tree is now restricted to the current account and SYSTEM. Credential, client-certificate, kubeconfig and rendered-Secret file ACLs were checked after the change. The development bootstrap now applies the explicit admin-only site-membership policy before reporting readiness.

The relay now pipelines bounded independent sends with per-event confirmation and mandatory-return checks; see [ADR 0019](../adr/0019-pipelined-publisher-confirmations.md). New component regressions cover all three findings and concurrent confirmations. The first build found a checked-exception type mismatch in trace error recording; that log is retained and the corrected full backend run is in progress. No new runtime performance or deployed security result is claimed yet.

Both console and API-type npm projects completed `npm ci --offline --no-audit --no-fund` using the prepared cache, installing 28 and 33 packages respectively. This proves those package installations were offline; the complete prepared runtime demonstration and cache acceptance checks remain pending.

At **16:20:18 Europe/Berlin**, the corrected owner-workflow verification passed all **83 checks** in 12 minutes 12 seconds. Combined with the unchanged shared/security, compatibility, messaging, simulator and adapter classes that passed in the preceding offline run, all **178 checks in 21 classes** pass with zero remaining failures, errors or skips. The initial owner fixture incorrectly requested a database budget below its schema minimum; the correction fills disposable PostgreSQL storage beyond a valid 16-MiB budget. Both original failure logs are retained. The console production build and generated API types also pass.

The updated README, [architecture guide](../architecture/overview.md) and [reviewer quickstart](../onboarding/quickstart.md) describe the implemented boundaries and actual unresolved results. The complete timed reviewer path is still unexecuted. A new scoped resilience driver covers separate adapter, application-database and monitoring outages; it is syntax-checked and awaiting deployment/execution. No new A24/A25/A45 acceptance is inferred from that source alone.

The clean `968cad5` application images were built and deployed with core V113. Independent simulator update retained world/generation and physical high water **4,137**. The first 120-second diagnostic (`load-diagnostic-1788877907651`) failed at **54.167% / 9,459.895 ms p99** across 600 measured movements, while all 650 warm-up/measured effects were single and quantities matched. Its worst retained trace shows prompt publisher confirmation, then consumer and scheduler waiting. Host available memory was about 1.0–1.8 GiB; VM available memory was about 7.5–7.9 GiB. Its final 40 seconds met the target, but that does not erase the failed diagnostic.

The separate warmed 60-second diagnostic (`load-diagnostic-1788878425958`) passed with **100% / 1,312.353 ms p99** across 300 measured movements, with all 350 effects single. Neither short run qualifies A50. The full ten-minute candidate is the next gate.

Explicit [N/N−1 producer fixtures](../../contracts/compatibility/README.md) now test both contract-consumer versions using frozen predecessor schema hashes and the current production decoder. Both optional-addition directions pass; changing integer quantity to a string is rejected by each consumer. Two new component checks passed offline at **16:45:02** in 19.637 seconds, bringing the verified set to **180 checks in 22 classes**. Initial harness failures involved an unavailable assertion helper and an incorrect expectation that the validator message included its property path; the original logs remain private. Actual predecessor-image operation remains a separate runtime gate.

### Interrupted load qualification — 8 September 2026

`load-1788878759277` did not qualify A50. The driver stopped after 689 completed HTTP offers because the open-loop schedule could not be maintained. Windows recorded a critical-battery trigger at **16:49:57 Europe/Berlin**, battery sleep at **16:50:01**, and the later standby exit at **16:57:43**; resource collection stopped across that interval. The machine subsequently reported external power and the named Cutover services remained running. The original requests, timing, resource error and failed results are preserved. No partial latency result is presented as a ten-minute pass. Sampler cleanup now consumes a closed-pipe send error without masking the primary failure, and future timing failures record the scheduled tick and actual lateness. The same full workload will be repeated.

### Sustained load and separate process outages — 8 September 2026

After restoring the managed console forward affected by host sleep, `load-1788880017964` completed the full 60-second warm-up and 600-second measurement on the `968cad5` runtime. It **failed A50: 85.9% within two seconds, p99 8,340.798 ms**, including all 3,000 measured movements with zero exclusions. All 1,980 offered requests were accepted and all 3,300 physical/business effects were single with matching quantities. HTTP acceptance p99 was 713 ms; driver lateness p99 was 17 ms. Five measured minutes passed individually while the others contained recurring stalls; those subwindows do not qualify the complete run. The retained trace `7ce88ea9011ef9631f9801048548f9ad` includes publication/consumption waits overlapping a database checkpoint. The source interval and all original failed results remain available. The preceding post-sleep attempt (`load-1788879941805`) failed during identity preflight with no offered work; restarting the owned console forward corrected that condition.

`authentication-1788880772251` passed the deployed security regressions on the same application images: actual shadow credentials cannot create orders/receipts but can read their permitted scheduling snapshot, owner records remain unchanged, all six APIs enforce site/JWT validity, forged source claims cannot mutate work, and untrusted equipment TLS clients fail. The originally valid token was rejected after its actual expiry plus permitted skew; the refreshed PKCE session still succeeded. World, journal generation and physical high water remained unchanged.

The first two scoped resilience attempts passed the separate adapter outage and then failed the database-outage response assertion. Cleanup restored every held controller and dispatch gate. The second attempt captured the exact fault: an empty **403** while the application database was absent. Shared problem handling covered jOOQ exceptions but omitted Spring's connection/transaction wrappers. Those now map to the documented **503 DATABASE_UNAVAILABLE**, preserving the same-key retry instruction and hiding internal connection details. Component and renewed runtime verification are required; no A25/A45 completion is inferred from the interrupted attempts.

[ADR 0021](../adr/0021-bounded-inbox-transactions.md) records bounded inbox transaction grouping to reduce per-delivery commit overhead while retaining acknowledgement-after-commit, nested handler savepoints and original replay identities. New real PostgreSQL/quorum regressions cover a crash before the first acknowledgement, later-member capacity failure rolling back every transfer, and mixed healthy/pending/raw-quarantine ownership. These changes remain under verification. The functional HTTP acceptance driver and 21 selected historical [runtime provenance records](../evidence/runtime-runs.json) have been added; neither is a substitute for the remaining executed acceptance checks.

At **17:39:39 Europe/Berlin**, all **41 durable-delivery checks** passed with zero failures, errors or skips (1 minute 21 seconds). The preceding run also passed all three new MVC checks for jOOQ, Spring data-access and Spring transaction exceptions returning a sanitized 503 problem with Retry-After. The first capacity-batch fixture compared PostgreSQL's spaced JSON text to jOOQ's compact wire form; the corrected fixture measures the relay's exact UTF-8 bytes and proves that one attempted effect rolls back when its peer cannot be retained. The original failed log remains. A full offline reactor verification is the next gate before packaging/deployment.

The full offline reactor completed at **17:53:26 Europe/Berlin** in **12 minutes 11 seconds**: **186 checks in 23 classes**, zero failures, errors or skips. This includes every owner workflow, all 41 durable-delivery checks, the three unavailable-database MVC cases, security, schema generation/upgrades and both contract-evolution checks. Production Java corresponds to `1f75e9e`; only reviewer/evidence documentation and its driver were added while verification ran. [Per-class counts](../evidence/backend-checks.json) are public; the original build log is private. Image build, simulator update and platform deployment follow before renewed runtime qualification.

Clean `cacff2d` images were built and deployed by **18:10:33 Europe/Berlin**, preserving the simulator world and journal generation. `platform-resilience-1788883850354` subsequently passed all three deployed checks: five original single effects after adapter absence; five accepted effects and recorded responses retained across application-database pod replacement; and 25 single movements while all four monitoring processes were stopped. Unavailable database intake now returns **503 DATABASE_UNAVAILABLE** and creates no order. Monitoring scrapes recovered, business pods survived, and all held controllers and dispatch controls were restored. This closes the runtime database-response regression; the separate sustained-load and queue-capacity gates remain open.

`queue-overflow-1788884331159` passed both actual quorum-capacity checks at **18:24:18 Europe/Berlin**. The critical queue rejected publication while retaining its original head and source event bytes, then drained **10,001 duplicate deliveries** without repeated effects. The independent shadow queue drained **1,001 duplicates** after both products continued under its full condition. Exhausted original events resumed through versioned supervisor replay; operator replay was denied. Exactly five physical effects were added, with unchanged world, generation and routes. Actual process-crash regressions are being repeated on the same runtime before offline qualification.

`process-crash-1788884703569` passed all three real core process exits on the `cacff2d` runtime: after business commit, after publisher confirmation, and after effect commit before acknowledgement. Each terminated with the deliberate exit code 73 and resumed its retained original; the latter two recorded duplicate transport with single effects. `mandatory-return-1788884905155` then passed at **18:28:46 Europe/Berlin**: the actual removed binding caused a positive-confirm mandatory return without falsely completing the outbox, and restoring that binding delivered the same event/movement IDs with one physical/sorting effect. Runtime batch crash, overflow and unroutable-publish regressions are now executed; the full performance measurement remains separate.

`cache-1788884948814` passed at **18:30:16 Europe/Berlin**. The prepared inventory verified **4,051 files**, including the tested predecessor image archive. An isolated empty-file-cache fixture returned the required failure and listed every missing tool, Maven/npm item, image archive and browser revision without deleting real caches or Docker images. This qualifies A49; actual prepared runtime operation under external-egress denial is running separately.

The first offline attempt (`offline-1788885032880`) stopped during network preflight before creating business work. Direct external connections were refused, but the proxy probe incorrectly treated an absent `HTTPS_PROXY` (exit 42) as a failed isolation assertion. Cleanup also rejected iptables' quoted representation of the expected rule comment. Inspection confirmed only the expected Cutover rules. Cleanup now normalizes that one quoting form and checks every line against the exact expected rule set; removing the owned chain restored the original DOCKER-USER hash exactly. Proxy probes now distinguish absent configuration from an actual refused connection, check both runtime containers and reject a successful connection or TLS-only failure as offline evidence. The failed run remains private and is not presented as an A48 pass.

The next offline attempt (`offline-1788885272986`) executed local login, both products with lost-response recovery, simulator restart, whole-lab stop/start with fresh PKCE login, verified archive import, predecessor rollback and telemetry under denial. Its dashboard screenshot captured headings while the panels were still loading, so it does not establish complete dashboard rendering. It **failed** its final browser-probe assertion: the application's restrictive CSP had rejected the injected fetch before the browser context's route handler could observe it. The corrected probe navigates a blank page in the same isolated context and requires the explicit network-disconnected error and denied route observation. A separate real-login check verified that correction with 26 route observations and one intentionally refused external navigation. The original run's cleanup restored the exact original firewall hash; the complete A48 rehearsal will be repeated with the corrected probe.

`offline-1788885775015` again passed the isolated business, simulator/whole-lab restart and predecessor-rollback portions, then failed dashboard rendering. Kubernetes recorded Grafana **OOMKilled**, exit 137, at **18:50:24 Europe/Berlin**, under its old **384 MiB** limit. The process loss also terminated its forward. That limit was below the [documented 512 MB evaluation minimum](https://grafana.com/docs/grafana/latest/setup-grafana/installation/). Grafana now has a bounded **1 GiB** limit and **768 MiB GOMEMLIMIT**; only that owned StatefulSet was updated. The dashboard check now explicitly returns to the requested dashboard after login, waits for six application targets, six memory series, both products' request series and all three rendered plots, and asserts that Grafana survives rendering. Visual inspection of the corrected runtime shows populated charts. A concurrent-browser stability check and full offline repeat follow; no A48 pass is inferred from the failed run. Its cleanup again restored the exact original firewall hash.

At **19:03 Europe/Berlin**, two real browser sessions passed the shared dashboard verifier and remained open through one minute of automatic refresh. All three panels rendered in each session; Grafana's restart count stayed zero. Sampled cgroup memory peaked at **683.36 MiB** under the new 1-GiB limit, confirming the old bound was inadequate. An initial simultaneous-login helper attempt failed before this observation; the repeated check established its sessions sequentially and then observed them concurrently. Human-login errors now identify only a fixed phase label, retaining the protection against exposing credential-fill arguments. Full isolated-runtime qualification is repeated after these corrections.

The next preflight (`offline-1788887039937`) failed before enabling denial because Chromium had changed its adjacent `debug.log`, which the cache inventory incorrectly treated as an installed asset. The browser inventory now excludes only the mutable `debug.log` and `chrome_debug.log` diagnostics while retaining hashes for installed runtime files. The original failed preflight is retained. Prepared/cold cache verification is repeated before the next offline walkthrough.

`cache-1788887175088` passed the corrected prepared and empty-file-cache checks with **4,050 installed assets**. The public source through `14a625d` was pushed after a bounded review of 455 tracked files, 854 reachable history blobs, current generated credentials and excluded paths/names; no finding was reported. All 122 relative links across 67 Markdown files resolved, the original planning-prefix hash stayed unchanged, and GitHub Actions remained disabled. Later dashboard/cache changes will receive the same final publication checks.

`offline-1788887275782` passed the complete A48 sequence at **19:15:04 Europe/Berlin**, including actual populated dashboard rendering and exact restoration of the original firewall hash. The [offline evidence report](../evidence/prepared-offline-2026-09-08.md) includes the inspected image and precise network boundary. `functional-1788887744619` then passed seven functional HTTP cases at **19:16:12**: compatible-zone single effects, original/fresh-key idempotency, changed-payload conflicts, admission refusal with original response retention, concurrent exact stock exhaustion, and invalid-input rejection without owner writes.

The functional driver's proxy fallback had accepted the correct 413 status while masking its HTML response. The proxy now returns the same bounded `PAYLOAD_TOO_LARGE` problem JSON as the application body filter, and the driver asserts raw response content type/body. Authentication-filter failures now also use bounded problem JSON, preserving the standard status and sanitized Bearer challenge. All **15 shared-platform checks** passed at **19:27:12** in 18.918 seconds, including four new filter-chain checks. The first fixture incorrectly expected 400 for a malformed bearer token; Spring classifies that credential as an invalid token and returns 401, which the corrected test preserves. Malformed JSON remains a separate 400 case. The original failed log remains. Full bundle verification, packaging/deployment and the new seven-case raw HTTP response driver are required before this correction is considered runtime-qualified.

`reset-bootstrap-1788887847395` passed at **19:32:08 Europe/Berlin**. It created and verified the quiescent checkpoint before stopping or removing the old dataset, preserved archives and unrelated resources, refused missing/stale reset flags, and created a fresh legacy-owned world with real login and both products completing. The exact prepared-image bootstrap took **604.419 seconds** (10 minutes 4 seconds); checkpoint/reset work is recorded separately. The full reviewer command sequence remains pending. Final shared-boundary response changes will be packaged and deployed before that sequence and the full healthy-load measurement.

The full offline reactor completed at **19:52:45 Europe/Berlin** in **17 minutes 7 seconds** on source `c6486a6e7918d704192e68d10fd03a078d60de00`: **190 checks in 24 classes**, zero failures, errors or skips. This includes all four new security-filter problem-response cases and every previous owner, reliability, schema and compatibility check. [Per-class results](../evidence/backend-checks.json) include the original log hash. The clean source images and console were built locally; the updated independent simulator preserves its world and journal generation. Platform rollout and actual raw-response verification follow before the timed reviewer path and sustained load.

The `c6486a6` images rolled out successfully at **20:25 Europe/Berlin**, with unchanged world, journal generation and existing owner data. `problem-responses-1788891930309` passed all seven [raw HTTP boundary checks](../evidence/http-boundaries-2026-09-08.md), including authentication-filter failures, operator role denial, malformed requests and the proxy's JSON 413. No core order, reservation or idempotency row was added. Source and documentation through `15a7d53` were published after review of 461 tracked files, 883 reachable Git blobs and all 44 current generated credentials; no finding was reported. All 130 relative documentation links resolved, the original planning prefix stayed unchanged, and GitHub Actions remained disabled.

`authentication-1788891939541` passed at **20:31:01 Europe/Berlin** on the same deployed images: all six owner APIs enforced real identity/site checks, the original valid token failed after actual expiry, refreshed PKCE remained valid, restricted shadow intake was denied, forged broker source claims caused no business change, and untrusted simulator client TLS failed. World, journal generation and physical high water stayed unchanged. `cache-1788892286758` then verified the final-image cache and explicit empty-file-cache failure across **4,050 assets**.

The first complete reviewer attempt, `reviewer-1788892359383`, passed all six exact commands in **269.326 seconds**. Its overall result nevertheless failed because the screenshot helper expected an `Overview` heading; the actual page heading is `The floor, in focus.` The corrected shared capture helper also selects completed tasks through the existing filter before waiting for populated rows. A separate real-browser check captured all five views successfully and every image was inspected. Application code and images are unchanged. Ambient is restored through the normal supervised reversal, retaining data and increasing its epoch, before a full corrected reviewer repeat; the original failed result is retained.

`reviewer-route-preparation-1788892858564` completed the normal ambient reversal with ten single effects and **294.602 ms** observed dispatch p99. The next reviewer run, `reviewer-1788892933362`, passed both product steps but stopped in shadow verification: its helper incorrectly expected the execution task table to be empty after a reversal, although thirteen completed historical tasks correctly remained. All 1,000 comparisons had matched. The helper now verifies both current legacy route owners, an unchanged set of retained execution task IDs, and no execution task for either new live legacy movement. Shadow still must have no execution tasks. No historical record is deleted; this corrects the verifier to support the documented preserved-data reversal path. The full reviewer sequence is repeated with the corrected assertion.

`reviewer-1788893203051` passed the [complete corrected rehearsal](../evidence/reviewer-walkthrough-2026-09-08.md) at **20:51:40 Europe/Berlin**. All six commands passed sequentially in **289.463 seconds**; all five actual populated console screenshots were captured and inspected, with no uncaught browser errors. World, journal generation and unrelated running containers were preserved. Runtime images remain `c6486a6`; helper source is `0f1eebf`. This closes A51/A54's final reviewer evidence, with the separate prepared-image bootstrap already recorded.

The first chilled load-preparation migration, `load-route-preparation-1788893520113`, correctly remained in OBSERVING because its first movement missed the fixed ten-movement latency gate: **3,009.643 ms after assignment**, **4,702.530 ms after original eligibility**. The other nine assignment-to-dispatch samples were 106.611–273.736 ms, and all ten effects were single. Trace `a4fc9d182cf675175e1370e99072dc8d` showed publication/consumption waits; this isolated observation does not establish a single root cause. `chilled-observation-reversal-1788893751603` used the normal supervised reversal and preserved the exact failed sample. The fresh attempt, `load-route-preparation-1788893781362`, completed ten single effects with **294.549 ms** observed dispatch p99. Both outbound zones are now active under execution ownership at epoch 3; no route, timestamp, observation or stock row was edited to force that result. The full healthy-load measurement runs separately.

`load-1788893862792` failed A50 at **21:10:44 Europe/Berlin**. Every one of 1,980 offered HTTP requests returned 202 and all 3,300 physical/business effects eventually completed once, but only **6.1%** of all 3,000 measured movements reached dispatch within two seconds of original eligibility; p99 was **156,213.575 ms**. No blocked, draining or unknown work was excluded. HTTP acceptance p99 was 1,023 ms, driver lateness p99 18 ms, and physical completion p99 165,436 ms. Synchronous commit and fsync remained enabled. The retained PostgreSQL runtime log contains **305 deadlock errors**, with direct cycles between adapter route rows and its shared outbox admission row. A multi-zone inbox batch could acquire a later route after retaining the admission lock from an earlier message. This is a confirmed batching regression, not evidence that the workload passed or that durability should be weakened.

The adapter now acquires its six seeded routes in site/zone order before an inbox movement handler can acquire the shared outbox budget. A deterministic two-transaction test waits for the actual PostgreSQL blocker relationship, then releases the competing writer. It failed on the old implementation at **21:19:45** (one inbox effect pending) and passed after the correction at **21:22:18** (both inbox effects and the competing allocation committed). [ADR 0021](../adr/0021-bounded-inbox-transactions.md) records the lock scope and tradeoff. The full adapter reactor and a fresh deployed measurement are still required; no performance pass is inferred from the component regression.

The corrected adapter reactor (`-o -pl apps/equipment-adapter -am verify`) passed all **56 checks across eight classes**, without failures, errors or skips, at **21:29:02 Europe/Berlin** in 156 seconds. This covers the technical starter, simulator and all adapter tests; the earlier 190-check whole-repository run remains separately versioned. [Exact scoped evidence](../evidence/adapter-lock-regression.json) identifies correction `8160ece` and all three retained log hashes. Clean images were built from that commit and are being deployed before runtime measurement.
