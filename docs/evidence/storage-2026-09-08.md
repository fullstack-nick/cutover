# Storage admission evidence — 8 September 2026

Source revision: `1b7f9a881a4333cf59b22b76870b5a830d803dd0` plus the reviewed restoration/storage changes. All 146 backend checks passed at 09:01:47 Europe/Berlin. Raw logs, exact JAR/image identities, diagnostic observations and invariant queries remain in ignored per-run evidence.

`volume-pressure-1788851160214` exercised a real core HTTP process using a restricted PostgreSQL runtime role on a disposable **384 MiB** tmpfs. The database container had a 768 MiB memory limit and one CPU limit; the host JVM used a 384 MiB maximum heap. Its executable JAR SHA-256 was `b3851a25e7c83a5a122b4fbf59d0355296cb874e7c5d0d3afbd854c2b1025f44`.

| Check | Observed result |
| --- | --- |
| Healthy filesystem | 358,096,896 available bytes; 80,530,636-byte reserve. |
| Allocated real filler pages | Available space fell to 50,864,128 bytes; diagnostics reported `CRITICAL`. |
| New HTTP order | `503 STORAGE_OBSERVATION`, `Retry-After: 2`; no second committed order. |
| Running scheduler | Recorded the physical-headroom barrier; zero equipment-boundary calls during pressure. |
| Accepted data and process restart | Original order/reservation remained readable across a cold JVM restart. |
| Reclaimed only fixture filler | Healthy observation returned; the original refused key accepted a second order; scheduling attempted its isolated endpoint again. |
| Final business assertions | Two orders, two retained reservations, zero inventory effects. |

This experiment connects no existing application database or live simulator. Its equipment endpoint is a counted refusal server, and message delivery is held through the normal controls. It establishes physical-space admission and retention behavior without claiming physical completion or load performance. Separate component checks prove that accepted completion updates remain writable, an expired/missing/malformed observation refuses new work, and a runtime user cannot read arbitrary server files or replace the observation function.

`volume-observation-1788851871227` then verified the deployed kind platform. All five owners reported the same actual filesystem observation, administrator requests succeeded and operator requests were refused. One two-line order and one three-classification receipt completed with five physical effects, two inventory effects and three sorting effects.

The test paused only the credential-free probe's exact containerd task. At observation ages 15.158–15.224 seconds, all five diagnostics reported `STALE`; both products refused intake with `503` and retained readable accepted work. Total order/receipt counts stayed at 131/10, and independent physical high water stayed at 193. The same probe resumed and fresh observations returned. No database, simulator or unrelated workload was paused to create this fault.

Early harness errors are retained: a thin-JAR invocation, absent diagnostics when messaging configuration was disabled, and a namespace-local signal that did not stop PID 1. They are not counted as passing runs. After deployment, 83 real network-policy checks and the ten-target telemetry check also passed. See [disk-pressure operations](../runbooks/disk-pressure.md) and [ADR 0015](../adr/0015-local-filesystem-headroom.md) for limits, permissions and recovery steps.
