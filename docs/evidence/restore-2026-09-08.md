# Application restoration evidence — 8 September 2026

These running-process experiments use source revision `1b7f9a881a4333cf59b22b76870b5a830d803dd0` plus the reviewed restoration changes. Private run artifacts retain the exact image digests, source/checkpoint manifests, table assertions, simulator evidence and lifecycle logs. The runtime is the local kind/Calico profile with its separate, continuously retained simulator database.

| Experiment | Observed result |
| --- | --- |
| `restore-1788846444578` | Six databases imported into a fresh cluster; all 211 table fingerprints matched. Replayed 1,184 original events. All 179 physical rows and 179 simulator command records matched; no unresolved finding. Explicit release took 581.973 seconds against the 900-second target. |
| `stale-restore-1788848295108` | One return movement was executing at capture. The source later completed it and three further movements. Restoring the older checkpoint reconciled the known movement once and quarantined the three missing intents. Release was refused. Simulator high water remained 183 throughout restoration. |
| `restore-1788849841860` | The complete rerun passed all four checks: fresh restoration and release, preserved-copy cold stop/start, restored PKCE login and both product read APIs, and safe source resumption/removal. All 211 table fingerprints matched; 1,207 original events replayed; 183 physical and 183 command records matched with no unresolved finding. Release took **648.572 seconds**. |

The stale run's checkpoint was captured at `06:18:35.923 UTC`; reconciliation observed current physical history at `06:27:12.439375 UTC`. Its measured RPO interval was **516.516 seconds**. The held restoration took **450.565 seconds**. Its missing physical-context count was three; a zero missing-command-context count does not imply an exhaustive inventory scan after quarantine. The source demo resumed with its later work intact, and the disposable restored copy was explicitly removed.

The three missing-context command identities were `2fff0064-14c9-4a1f-93e9-7f8aae2d13d3`, `86221444-c3db-4369-9cc4-2439be58d3b1` and `a33bd63f-7e58-3c1c-bc33-2d59ab1da59e`. No replacement movement or missing business record was invented. A47 demonstrates detection and quarantine of the recovery gap, not recovery of the absent orders/receipts.

The first quiescent run also completed real local PKCE supervisor login and read both product APIs through the restored console. Its return-to-demo step failed: cached Kubernetes Ready status still described processes from before the node stopped, so the console forward opened too early. The corrected lifecycle additionally requires ready container processes started after the Docker node's current start. A warm repair passed, and the subsequent stale experiment successfully exercised a cold source restart. The first run's failure remains recorded; it was not rewritten as a wholly passing run.

A further cold lifecycle attempt, `restore-1788849213425`, failed before importing databases because CRI did not immediately recognize the freshly tagged Grafana digest. The original demo resumed safely. Image verification now waits up to 15 seconds for CRI's asynchronous image observation; it still requires the exact recorded digest. The complete rerun above passed with that correction. Its restored copy resumed at `06:58:11.453 UTC`; the original demo resumed at `07:04:53.031 UTC`, preserving original storage and physical history.

Reproduce with `node tools/scenario-driver/restore-smoke.mjs` and `node tools/scenario-driver/stale-restore-smoke.mjs` after the documented local platform is healthy. These scripts create named private checkpoints and separate restoration storage; read the [restore runbook](../runbooks/restore.md) first. Same-host checkpoints do not protect against host-disk loss. Prepared offline operation, the full reviewer recording and other A01–A54 scenarios have their own acceptance gates.
