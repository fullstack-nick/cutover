# Independent returns — 8 September 2026

This records the second-product milestone on the local kind demonstration platform. It is selected execution evidence, not the final A01–A54 acceptance bundle. Source revision was `551b628e9ef9ab4805f1a46903623d439431e96e` plus the accompanying uncommitted returns implementation. Raw manifests, machine paths, logs and credentials remain in ignored local storage.

## Executed checks

| Check | Observed result |
| --- | --- |
| Focused returns build | 13 initial connected checks passed in 1 minute 27 seconds at 05:11 Europe/Berlin. Its reactor excluded core and execution. |
| Full Maven verification | 121 checks passed, with no failures, errors or skips, in 7 minutes 53 seconds at 05:24:51. This includes 16 expanded returns checks and the existing 1,000 persisted shadow comparisons. |
| Local deployment | Five application migration Jobs plus the identity migration completed; returns started with V116 and its own runtime role. All expected workloads became ready. |
| `returns-1788838277326` | Five process checks passed at 05:32:27 through HTTP, Keycloak, RabbitMQ, actual service processes and the independent simulator. |
| `returns-ui-1788838385165` | Two real PKCE/browser checks passed, including supervisor reconciliation of a quarantined sorting command. |
| `returns-ui-1788838637610` | The same browser checks passed after wording/evidence-panel polish, with new screenshots at 1440×1050 and 390×844. |
| `telemetry-1788838441512` | All ten expected targets scraped successfully; an application trace was retrieved from Tempo; local Grafana reported a healthy database. |
| `policy-1788838595392` | 83 actual resolver/TCP checks passed across core, adapter, execution, shadow, returns, proxy and telemetry identities. Temporary probe pods were removed. |

The expanded network check includes the adapter's intentional owner-proof connections to core and execution. Returns can reach its database server, broker, identity, adapter and collector; it cannot reach the simulator directly, core or execution. SQL roles independently restrict which database an application may use. A TCP allow result alone is not a database-permission claim.

## Business and failure evidence

Receipt `94ee325d-d035-421f-a43f-3ea93e24b039` received eight reusable, three cleaning and two damaged crates. Five concurrent requests with new idempotency keys returned the same receipt. Changed payloads conflicted. Six repeated broker messages used new event IDs but retained their original semantic stream positions. The three movement/physical/sorting effects remained single, with exactly 13 received and 13 sorted crates. Core inventory did not change during this returns-only check.

The lane-fault check blocked one ambient lane while both an ambient/chilled order and a separate returns receipt completed. Actual outbound command evidence used a compatible unblocked lane. The restart check held a returns task on its blocked lane, stopped only the returns deployment, completed an outbound order while that product was unavailable, then restarted returns and confirmed the original receipt, task and movement IDs. Removing the lane fault completed one physical sorting effect.

A selected lost-response fault produced an adapter unknown-outcome event before the returns receipt completed through the original command's retained evidence. The browser walkthrough separately created a history-gap quarantine: an operator had no recovery action, while a supervisor supplied a reason and requested reconciliation. Each walkthrough ended with one physical execution and one sorting-ledger entry.

Component checks additionally verify changed-payload conflicts, invalid classifications, concurrent registration, new semantic stream versions, inbox/effect rollback, complete registration rollback after an outbox fault, late observations, partial receipt uncertainty, retry exhaustion and audited recovery, checkpoint barriers and empty-poll write avoidance. These use three disposable owner databases and deterministic transport ports, and are distinguished from the process evidence above.

## Identity and retained state

The four scaffold template hashes were rechecked against their recorded provenance. The runtime returns image was:

```text
docker.io/cutover/returns-service@sha256:605da480afba27590f3fc97c553cfeaa365bf8d06cdb7b1cfedaa85f5ff22d61
```

The final console image was:

```text
docker.io/cutover/operations-console@sha256:60bfe76a3cd84f4d5b38c2a7ff0b382fe8a9e312d5ca3f40e3e510c988ba317c
```

Postflight at 05:39:47 confirmed six completed receipts, ten sorting-ledger entries, no pending/quarantined returns inbox entries, and 19 reusable / five cleaning / three damaged crates received and sorted. All return-counter outstanding values were zero. Controls were open and no outbound migration session remained active.

The simulator world `3b9c9c76-96ea-43e3-b009-af0c97939b35` and journal generation `d9f73d93-eff7-4cc7-a0db-238a6ff24571` were preserved; its physical sequence reached 179. Site-a ownership remained execution for ambient at epoch 1 and chilled at epoch 5, with returns at epoch 0. These small controlled runs do not establish the final mixed-product load target, physical storage-pressure behavior, backup recovery or offline readiness.
